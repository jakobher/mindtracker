<div class="max-w-md mx-auto px-4 py-12">
    <div class="bg-white rounded-xl shadow-md border border-warm-200 p-8 mb-6">
      <h1 class="text-2xl font-bold mb-3 text-warm-800">Skapa konto</h1>
      <p class="text-warm-600">Registrera dig för att börja hantera dina exponeringsövningar och tankeutmaningar.</p>
    </div>
  
    <div class="bg-white rounded-xl shadow-md border border-warm-200 overflow-hidden">
      <form action="<%= baseURL %>auth/register" method="POST" class="p-6">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        
        <div class="mb-4">
          <label for="username" class="block text-warm-700 font-medium mb-2">Användarnamn</label>
          <input 
            type="text" 
            id="username" 
            name="username" 
            class="w-full px-4 py-2 border border-warm-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-warm-400"
            required 
            autofocus
          >
          <p class="mt-1 text-sm text-warm-500">Minst 3 tecken. Endast bokstäver, siffror och understreck.</p>
        </div>
        
        <div class="mb-6">
          <label for="password" class="block text-warm-700 font-medium mb-2">Lösenord</label>
          <input 
            type="password" 
            id="password" 
            name="password" 
            class="w-full px-4 py-2 border border-warm-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-warm-400"
            required
          >
          <p class="mt-1 text-sm text-warm-500">Minst 10 tecken långt.</p>
        </div>
        
        <div class="flex items-center justify-between">
          <button 
            type="submit" 
            class="bg-warm-600 hover:bg-warm-700 text-white font-medium py-2 px-6 rounded-lg transition duration-300"
          >
            Registrera
          </button>
          <a href="<%= baseURL %>" class="text-warm-600 hover:text-warm-800 font-medium">Avbryt</a>
        </div>
      </form>
    </div>
    
    <div class="text-center mt-4 text-warm-600">
      <p>Har du redan ett konto? <a href="<%= baseURL %>auth/login" class="text-warm-700 hover:text-warm-900 font-medium">Logga in här</a></p>
    </div>
  </div>